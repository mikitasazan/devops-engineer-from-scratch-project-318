# Observability evidence

Use this directory for Grafana dashboard and alert screenshots after the
monitoring VM is deployed. The dashboard and provisioning sources are kept in
`monitoring/grafana/` so the visual state can be recreated at any time.
